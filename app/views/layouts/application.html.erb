<!DOCTYPE html>
<html>
  <head>
    <title>Amarket</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload', as: 'style' %>
    <%= javascript_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'admin', media: 'all', 'data-turbolinks-track': 'reload' %>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
  </head>

  <body class="hold-transition sidebar-mini layout-fixed">
    <div class="wrapper">
      <% if signed_in? %>
        <%= render 'layouts/navbar' %>
      <% end %>
      <div class="content-wrapper">
        <section class="content">
          <div class="container-fluid">
            <%= render 'shared/flash_message' %>
               <%= params[:controller] %>
            <%= yield %>
          </div>
        </section>
      </div>
      <footer class="main-footer">
        <div class="float-right d-none d-sm-inline-block">
          <b>Version</b> 3.2.0
        </div>
        <strong>Copyright &copy; <%= Time %> <a href="https://adminlte.io">Amarket.io</a>.</strong>
        All rights reserved.
      </footer>
    </div>
  </body>
</html>
