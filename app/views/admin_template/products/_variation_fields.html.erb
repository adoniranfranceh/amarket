<div class="col-md-6 div-center">
  <div class="nested-fields card card-success">
    <div class="card-header">
      <h3 class="card-title">Variação</h3>
    </div>
    <div class="card-body">
      <div class="row">
        <%= f.text_field :name, placeholder: :name, class: 'form-control col-3 mr-4' %>
        <%= f.text_field :color, placeholder: :color, class: 'form-control col-3 mr-4' %>
        <%= f.text_field :type, placeholder: :type, class: 'form-control col-3 ' %>
      </div>
      <br>
      <% if action_name != 'show' %>
        <div class="custom-file">
          <%= f.file_field :photo, class: 'custom-file-input', id: 'exampleInputFile' %>
          <%= f.label :photo, class: 'custom-file-label', id: 'exampleInputFile' %>
        </div>
        <%= link_to_remove_association('Remover', f, class: 'btn btn-danger') %>
      <% end %>

      <div id="subgroups">
        <h3><%= is_present?(@subgroups, 'Subgrupo', f, :subgroups,'btn btn-primary mb-3') %></h3>
        <%= f.fields_for :subgroups do |subgroup| %>
          <%= render partial: 'subgroup_fields', locals: {f: subgroup}  %>
        <% end %>
      </div>
    </div>
  </div>
</div>
