<%= simple_form_for [:admin_template, @admin], html: { class: 'form' } do |form| %>
  <%= form.fields_for :profile_admin do |profile_admin_fields| %>
    <div class="form-group row">
      <div class="col-md-6">
        <%= profile_admin_fields.input :first_name, class: 'form-control', placeholder: 'Primeiro Nome' %>
      </div>
      <div class="col-md-6">
        <%= profile_admin_fields.input :last_name, class: 'form-control', placeholder: 'Sobrenome' %>
      </div>
    </div>
  <% end %>
  <div class="form-group">
    <%= form.input :email, label: false, placeholder: 'Endereço de Email', class: 'form-control' %>
  </div>
  <div class="custom-file form-group">
    <%= form.file_field :avatar, class: 'custom-file-input', id: 'exampleInputFile' %>
    <%= form.label 'Escolha uma imagem', class: 'custom-file-label', id: 'exampleInputFile' %>
  </div>
  <div class="form-group">
    <%= form.submit 'Salvar', class: 'btn btn-primary' %>
  </div>
<% end %>
